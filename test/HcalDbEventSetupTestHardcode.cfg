# Configuration file for EventSetupTest_t

process TEST = {

	path p = { print & HcalDbAnalyzer}


	module print = AsciiOutputModule { }
	
	module HcalDbAnalyzer = HcalDbAnalyzer {}
	
	es_module = HcalDbProducer {}

#	es_source = HcalHardcodeCalibrations {untracked vstring toGet = {"Pedestals", "PedestalWidths", "Gains", "GainWidths"}}
	es_source = HcalHardcodeCalibrations {untracked vstring toGet = {"Pedestals", "PedestalWidths", "Gains"}}


	source = EmptySource {untracked int32 maxEvents = 2
                              untracked uint32 firstRun = 1 
                              untracked uint32 numberEventsInRun = 1 
                                   }

}
